#=========================================
#config
#=========================================

# Scheduler for updating system accounts
# thread pool size (should core number x 10 max)
thread.pool.size=32